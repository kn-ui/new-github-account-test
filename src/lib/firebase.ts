// Firebase client removed. Use Clerk for auth and backend API/Hygraph for data.
export const auth = undefined as unknown as never;
export const db = undefined as unknown as never;
export default {} as never;